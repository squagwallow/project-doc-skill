project_slug: project-doc-skill
doc_type: entry
updated_at: 2026-04-17
url: https://cdn.jsdelivr.net/gh/squagwallow/project-doc-skill@main/entry.md

# project-doc-skill — Entry Document
Type: META | Bootstrap document. Read before every session.

## Purpose
Designing, building, and iterating a reusable LLM skill called build-new-project.skill that initializes a complete, LLM-readable external memory and documentation layer for any new project. The skill interviews the user, scopes the project, engineers all required prompts, and generates a complete repo ready to deploy. This project is simultaneously the first project running on its own protocol.

## Current Objective
Test skill v0.5 in Claude Code against a real project. Iterate based on results.

## Roadmap

### Track A — Bootstrap (current)
- ✅ entry.md published to jsDelivr
- ✅ thread-handoff-protocol.md committed
- ✅ build-new-project.skill.md v0.5 drafted and committed
- ⏳ Test v0.5 in Claude Code against a real project
- ⏳ Declare v0.1 working

### Track B — Skill iteration
- Test v0.5 on job-search project in Claude Code
- Test v0.5 on MFT career project
- Iterate to v1.0 stable

### Track C — System extensions (future)
- Personal Context Layer design
- Versioning protocol for retrofitting existing projects
- Agentic prompt testing within Claude Code
- MCP write-tool automation for notes

## Settled Decisions
- Thread-start prompt is the primary retrieval mechanism — LLM-agnostic.
- GitHub is the authoring layer only. All LLM consumption routes through jsDelivr CDN.
- entry.md hard size limit is a performance requirement — context rot is real.
- Exit protocol equals manual compaction — model drafts, human reviews and commits.
- Settled decisions use "Defaults — Override With Cause" framing.
- Conditional reading must include explicit trigger conditions and full jsDelivr URLs.
- Pre-task checklist belongs in both entry.md and thread-start prompt.
- AGENTS.md must be human-authored.
- Personal Context Layer is conditional required reading only — one central document.
- jsDelivr via GitHub is the confirmed retrieval host.
- raw.githubusercontent.com is unverified for robots.txt — do not use until tested.
- Each project generated by the skill gets its own separate GitHub repo.
- The skill runs in Claude Code — generates a complete folder, not text blocks.
- Prompt engineer skill absorbed into prompt-engineer-entry.md per project.
- Tiers are examples not fixed categories — the skill is adaptive.
- Decomposition applies to all project types regardless of complexity.
- Notes block is on by default with trigger-based activation.
- GitHub project connector is a viable retrieval path for Claude.ai sessions.
- Claude Code FILE:/DELETE:/COMMIT MESSAGE: format is the settled commit mechanism.
- Handoff log must include do-not-retry section and handoff completeness rules.
- Writing prompts must use two-pass draft pattern and evidence-based self-review.
- Style guides must use contrastive bad→good pairs for every prohibition.
- README.md is standard skill output generated at project creation.

## Defaults — Override With Cause
- Do not propose GitHub connector or Notion MCP as primary retrieval paths.
- Do not propose expanding entry.md into full documentation.
- Do not propose Netlify as documentation host.
- Do not propose nesting generated projects inside this repo.
- Do not propose fixed template taxonomy — the skill is adaptive.
- Do not use relative paths in conditional reading — always full jsDelivr URLs.

## Current Build State
Complete: Core architecture, all settled decisions, thread-handoff-protocol v0.1, retrieval host confirmed, skill v0.5 drafted and committed, job-search repo generated and actively used as first real test case.
In progress: v0.5 Claude Code test.
Blocked: Nothing.
Not started: AGENTS.md template spec, full skill document, full roadmap doc, Personal Context Layer, Track C items.

## Open Questions
- Notes block cadence needs real-world validation — trigger logic designed but testing ongoing.
- Exit protocol low-friction design partially resolved — completeness rules added, still evolving.
- Personal Context Layer not yet designed.
- raw.githubusercontent.com robots.txt status unverified.
- Versioning protocol for retrofitting existing projects — noted, not designed.

## Conditional Reading
Fetch only if the current session objective requires it.

- build-new-project.skill.md →
  https://cdn.jsdelivr.net/gh/squagwallow/project-doc-skill@main/skill/build-new-project.skill.md
  Fetch if: working on the skill document, reviewing skill output, or running a test.

- Thread Handoff Protocol v0.1 →
  https://cdn.jsdelivr.net/gh/squagwallow/project-doc-skill@main/thread-handoff-protocol.md
  Fetch if: designing or revising the handoff or compaction protocol.

- Encyclopedia: LLM Prompt & Context Design Principles →
  https://elaborate-belekoy-9e0eef.netlify.app/llm-design-encyclopedia
  Fetch if: designing or auditing any template field, prompt, trigger condition, session handoff, compaction, or entry.md structure decisions.

## Notes Block
Maintain throughout sessions per these rules.

Triggered on:
- First major decision point → prompt: "we're starting to make decisions — want me to start taking notes?" If yes, activate.
- Thread getting long → prompt: "this thread is getting substantial — want me to start tracking notes and context?" If yes, activate.
- User says any of: "note that", "keep track", "where are we", "give me an update", "what's our status" → activate immediately.
- A decision is made, task completed, or state-changing action occurs → update if already active.

Once active: stays on until user says "pause notes".

Format:
─── THREAD NOTES ──────────────────
✓ [Settled decisions]
→ [Active tasks]
? [Open questions]
⚑ [Flagged for next thread]
Context: [0–25% | 25–50% | 50–75% | 75%+ compact soon]
───────────────────────────────────

Append to responses only when the block changes.

## Revision Mode
If the user says "inspect repo", "update system", "revise [file]", or any variant indicating they want to change the documentation itself:
- Switch to system maintenance mode
- Load the relevant file
- Propose the change before implementing
- Output the full updated file for the user to commit
- Update prompt-engineer-entry.md if the change affects any prompt or orchestration relationship
- Confirm committed before resuming project work

## No-Directive Default
If the user loads this entry with no directive, present a brief orientation from docs/user-guide.md and ask what they want to do.

## Pre-Task Checklist
Before starting any session:
- [ ] I have read the Current Objective
- [ ] I have read all Settled Decisions — I will not re-open these
- [ ] I have read the Defaults list — I will not re-propose these
- [ ] I have fetched all Required Reading documents listed above
- [ ] RETRIEVAL GATE: If any Required Reading document failed to fetch, stop and report before proceeding.

## Thread Exit Protocol
Before ending any session:
1. Model drafts all updated fields — human reviews before committing.
2. Update Current Build State precisely.
3. Update Open Questions — add new, remove resolved.
4. Append any new Settled Decisions.
5. Set next Current Objective as a single sentence.
6. Update updated_at.
7. Output changed files in Claude Code FILE:/DELETE:/COMMIT MESSAGE: format.
8. Confirm entry.md is still under 4,000 tokens.
