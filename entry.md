project_slug: project-doc-skill
doc_type: entry
updated_at: 2026-04-19
url: https://raw.githubusercontent.com/squagwallow/project-doc-skill/main/entry.md

# project-doc-skill — Entry Document
Type: META | Bootstrap document. Read before every session.

## Purpose
Designing, building, and iterating a reusable LLM skill called build-new-project.skill that initializes a complete, LLM-readable external memory and documentation layer for any new project. The skill interviews the user, scopes the project, engineers all required prompts, and generates a complete repo ready to deploy. This project is simultaneously the first project running on its own protocol.

## Current Objective
Run the end-to-end session test on the job-search v1-notion-mcp prototype — load entry doc in fresh Claude Desktop chat, issue a real directive, verify Notion queue write. Declare v1 working or log friction.

## Roadmap

### Track A — Bootstrap
- ✅ entry.md published
- ✅ thread-handoff-protocol.md committed
- ✅ build-new-project.skill.md v0.5 drafted and committed
- ✅ Full repo structure committed
- ✅ v1 architecture validated (git + Notion MCP) on job-search prototype
- ✅ skill v1.0 committed with full Notion MCP layer
- ⏳ End-to-end session test on job-search v1 prototype
- ⏳ Declare v1 working

### Track B — Skill iteration
- ✅ skill v1.0 rewritten with git + Notion architecture
- Test v1.0 skill on a fresh project (MFT career or other)
- Iterate to v1.1 based on test findings

### Track C — System extensions (future)
- Personal Context Layer design
- Versioning protocol for retrofitting existing projects
- Agentic prompt testing within Claude Code
- Playwright MCP integration for browser-based job search

## Settled Decisions
- Thread-start prompt is the primary retrieval mechanism — LLM-agnostic.
- GitHub connector/direct repo access is the primary retrieval path when available.
- raw.githubusercontent.com is confirmed working as retrieval URL — now the default for v1 generated projects.
- jsDelivr is a known fallback retrieval path, not the default.
- entry.md hard size limit is a performance requirement — context rot is real.
- Exit protocol equals manual compaction — model drafts, human reviews and commits.
- Settled decisions use "Defaults — Override With Cause" framing.
- Conditional reading must include explicit trigger conditions.
- Pre-task checklist belongs in both entry.md and thread-start prompt.
- AGENTS.md must be human-authored.
- Personal Context Layer is conditional required reading only — one central document.
- Each project generated by the skill gets its own separate GitHub repo.
- The skill runs in Claude Desktop with GitHub MCP — generates files directly via MCP commits.
- Prompt engineer skill absorbed into prompt-engineer-entry.md per project.
- Tiers are examples not fixed categories — the skill is adaptive.
- Decomposition applies to all project types regardless of complexity.
- Claude Code FILE:/DELETE:/COMMIT MESSAGE: format is the settled commit mechanism for Claude Code sessions.
- Handoff log must include do-not-retry section and handoff completeness rules.
- Writing prompts must use two-pass draft pattern and evidence-based self-review.
- Style guides must use contrastive bad→good pairs for every prohibition.
- README.md is standard skill output generated at project creation.
- v1 deliverable architecture is git + scoped Notion MCP — validated 2026-04-19.
- Notion internal integration token (not managed OAuth) is the correct Notion access mechanism.
- One Notion integration per project — hard scoping at the access layer.
- THREAD NOTES block is always-on with turn counter — never trigger-based.
- Notion database creation requires one manual step — LLM populates schema via API-update-a-data-source.
- Encyclopedia conditional reading is scoped to revision mode only — not a general session resource.
- Claude Desktop is the primary LLM surface for v1 — Perplexity is the mobile/web fallback.

## Defaults — Override With Cause
- Do not propose expanding entry.md into full documentation.
- Do not propose Netlify as documentation host.
- Do not propose nesting generated projects inside this repo.
- Do not propose fixed template taxonomy — the skill is adaptive.
- Do not propose managed OAuth Notion connector — use internal integration token only.
- If GitHub connector/direct repo access is unavailable, ask whether to switch to jsDelivr before inventing another retrieval path.

## Current Build State
Complete: Core architecture, all settled decisions, thread-handoff-protocol v0.1, retrieval host confirmed, skill v1.0 committed with full git + Notion MCP architecture, job-search v1-notion-mcp branch deployed with Notion layer live (Flagged Jobs + Writing Samples databases seeded), all documentation updated.
In progress: End-to-end session test on job-search v1 prototype.
Blocked: Nothing.
Not started: AGENTS.md template spec, full roadmap doc, Personal Context Layer, Track C items.

## Open Questions
- End-to-end session test not yet run — does the LLM correctly load entry doc, read Notion queue, and write back without prompting?
- Playwright MCP integration for browser-based job search — not yet explored.
- Versioning protocol for retrofitting existing projects — noted, not designed.
- Platform research pending: optimal documentation host for paid client deliverables.

## Conditional Reading
Fetch only if the current session objective requires it.

- build-new-project.skill.md →
  https://raw.githubusercontent.com/squagwallow/project-doc-skill/main/skill/build-new-project.skill.md
  Fetch if: working on the skill document, reviewing skill output, or running a test.

- Thread Handoff Protocol v0.1 →
  https://raw.githubusercontent.com/squagwallow/project-doc-skill/main/thread-handoff-protocol.md
  Fetch if: designing or revising the handoff or compaction protocol.

- Encyclopedia: LLM Prompt & Context Design Principles →
  https://elaborate-belekoy-9e0eef.netlify.app/llm-design-encyclopedia
  Fetch if: in revision mode editing any prompt, trigger condition, or entry.md structure.

## Notes Block
Maintain a THREAD NOTES block in every reply. Update before writing the main response.

Update order:
Prune: Remove items that are obsolete or irrelevant.
Merge: Combine related points into one short clause.
Tighten: Rewrite for maximum brevity and clarity.
Add: Only add new items important for future turns.

Format:
THREAD NOTES (Turn N)
✓ Settled: brief decisions, separated by · where possible
→ Active: current tasks / work in progress (max 3 items)
? Open: unresolved questions / forks (max 5 items)
Δ Meta: optional constraints/observations (omit line when empty)

Rules:
Always update THREAD NOTES first, then write main reply.
Keep the block as short as possible while still accurate.
Prefer deleting and merging over letting it grow.
No emojis, bold, markdown headings, or bullet lists inside THREAD NOTES.
Use inline separators (·) instead of extra line breaks.
When Turn N > 20, be extra aggressive about pruning; suggest thread rotation if crowded.

## Revision Mode
If the user says "inspect repo", "update system", "revise [file]", or any variant indicating they want to change the documentation itself:
- Switch to system maintenance mode
- Load the Encyclopedia before proposing any prompt or structure change
- Load the relevant file
- Propose the change before implementing
- Output the full updated file for the user to commit
- Update prompt-engineer-entry.md if the change affects any prompt or orchestration relationship
- Confirm committed before resuming project work

## No-Directive Default
If the user loads this entry with no directive, present a brief orientation from docs/user-guide.md and ask what they want to do.

## Pre-Task Checklist
Before starting any session:
- [ ] I have read the Current Objective
- [ ] I have read all Settled Decisions — I will not re-open these
- [ ] I have read the Defaults list — I will not re-propose these
- [ ] I have fetched all Required Reading documents listed above
- [ ] RETRIEVAL GATE: If any Required Reading document failed to fetch, stop and report before proceeding.

## Thread Exit Protocol
Before ending any session:
1. Model drafts all updated fields — human reviews before committing.
2. Update Current Build State precisely.
3. Update Open Questions — add new, remove resolved.
4. Append any new Settled Decisions.
5. Draft a docs/work-log.md entry capturing any design, research, development, or deployment work done this session.
6. Set next Current Objective as a single sentence.
7. Update updated_at.
8. Commit all changed files via GitHub MCP.
9. Confirm entry.md is still under 4,000 tokens.
